# Reference Caddyfile block for Watchlistr
# Copy and adapt this block into your server's global Caddyfile:

watchlistr.example.com {
    # Enable modern response compression (Zstd & Gzip)
    encode zstd gzip

    # Path to static frontend dist/ files inside Caddy container volume
    root * /srv/watchlistr

    # Reverse proxy TVDB API requests to the ttvdb-proxy Docker container
    handle /api/* {
        reverse_proxy ttvdb-proxy:3000
    }

    # Serve static frontend SPA assets with index.html fallback
    handle {
        try_files {path} /index.html
        file_server
    }
}

# Reference block for Nightly release served on a subpath (e.g. nightly.another.one/watch):
# nightly.exampl.com/watchlistr {
#     encode zstd gzip
#
#     # Reverse proxy TVDB API requests for the /watch subpath
#     handle /watchlistr/api/* {
#         uri strip_prefix /watchlistr
#         reverse_proxy ttvdb-proxy-nightly:3000
#     }
#
#     # Serve static nightly frontend dist under /watch
#     handle_path /watchlistr/* {
#         root * /srv/watchlistr-nightly
#         try_files {path} /index.html
#         file_server
#     }
# }
